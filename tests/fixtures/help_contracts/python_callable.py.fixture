def combine(left: float, right: float) -> tuple[float, float]:
    """
    Combine two values.

    Examples
    --------
    >>> combine(1.0, 3.0)
    (2.0, 2.0)

    >>> combine(2.0, 4.0)
    (3.0, 3.0)
    """

    return (left + right) / 2.0, (left + right) / 2.0
